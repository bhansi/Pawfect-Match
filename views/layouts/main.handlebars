<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pawfect Match</title>
  <link rel="stylesheet" href="css/style.css">
</head>
<body>
  <header class="mainContainer">
    <h1>Pawfect Match</h1>
    <!-- User Welcome Message -->
    {{#if logged_in}}
      <div class="user-welcome">
        <p>Welcome,{{userName}}!</p>
        <button id='logout'> Log out</button>
      </div>
    {{/if}}
    {{#if showNavBar}}
    <section class="navBar">
      <a href="http://localhost:3001/login"><button>Log In</button></a>
      <a href="http://localhost:3001/login"><button>Sign up</button></a>
      {{!-- <a href="http://localhost:3001/"><button>Log out</button></a> --}}
    </section>
    {{/if}}
  </header>
  <main>
   {{#if showDogs}}
  {{> dogs }} <!-- Include the dogs partial -->
{{/if}}

{{#if showCats}}
  {{> cats }} <!-- Include the cats partial -->
{{/if}}
    {{{body}}}
</main>
<footer>
  <h3> 🐾 This application has been created by © 2023 Team 3 🐾 </h2>
</footer>  
</body>
</html>