const router = require('express').Router();
const { Adoptions } = require('../../models');
const withAuth = require('../../utils/auth');

router.get('/applications', async (req, ))

module.exports = router;